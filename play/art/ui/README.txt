Drop UX interface PNGs here. The game loads them automatically by exact filename:

  backdrop.png      — studio backdrop (behind the whole broadcast)
  status-bar.png    — top HUD bar plate (wave label · timer · essence)
  lower-third.png   — name banner (wiring pending placement)
  tv-frame.png      — CRT bezel (note: better used on the landing page, not in-canvas)

Filenames must match exactly. See dungeoncrawlers.tv/ux-assets.html for the full list.
